import { WebSocketExceptionsFilter } from './webSocketExceptions.filter';

describe('WebSocketExceptionsFilterFilter', () => {
  it('should be defined', () => {
    expect(new WebSocketExceptionsFilter()).toBeDefined();
  });
});
